<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/happybrain2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/happybrain2"
        android:supportsRtl="true"
        android:theme="@style/Theme.ANDROID_MATH_BRAIN">

        <activity
            android:name=".RankedActivity"
            android:exported="false" />
        <activity
            android:name=".EndLevelActivity"
            android:exported="false" />
        <activity
            android:name=".LevelActivity"
            android:exported="false" />
        <activity
            android:name=".AdventureActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MusicService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>